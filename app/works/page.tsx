export default function WorksPage() {
  return (
    <section style={{
      minHeight: '100svh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 'var(--px)',
      paddingTop: 'calc(var(--nav-height) + var(--space-8))',
    }}>
      <h1 style={{ fontSize: 'var(--text-4xl)' }}>Works</h1>
    </section>
  );
}
